import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally minimal for now. Add image domains, headers, etc.
  // here as the app needs them. No config is required for a default
  // deploy to Vercel — it auto-detects Next.js from package.json.
};

export default nextConfig;
